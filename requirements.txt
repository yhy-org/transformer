To ensure the proper execution of the image classification project using CNN and Transformer architectures on CIFAR-100, the following packages need to be installed:
torch>=1.7.0
torchvision>=0.8.0
tensorboard>=2.4.0
numpy>=1.19.2
You can install these packages via pip using the following command:
pip install torch torchvision tensorboard numpy
